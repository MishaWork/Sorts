int Finder (int Mass [], int SortSize, int SortElem)
    {
    for (int Count = 0; Count < SortSize; Count ++)
        {
        Compare = Compare + 1;

        if (SortElem < Mass [Count])
            {
            return Count;
            }
        $c

        //printf (" Count > %d <", Count);

        //printf (" Mass [Count] > %d <", Mass [Count]);

        //printf (" SortElem > %d <\n", SortElem);
        }

    return SortSize;
    }
